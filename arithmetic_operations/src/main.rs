fn main() {
    // some basic arithmetic operations in Rust
    // addition
    let mut a = 5;
    let mut b = 20;

    let sum = a + 20;

    // subtraction
    let subtraction = 89.5 - 56.7;

    // multiplication
    let product = 4 * 30;

    // division
    let quotient = 56.7 / 32.2;
    let truncated = -5 / 3;

    // remainder
    let remainder = 43 % 5;

    println!("The sum of {a} and {b} is {sum}");
}
